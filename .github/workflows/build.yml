name: Build and Release ollama-tui

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt --use-deprecated=legacy-resolver
          pip install pyinstaller

      - name: Build executable with PyInstaller
        run: |
          pyinstaller --onedir `
            --exclude-module nltk `
            --exclude-module scipy `
            --name "Ollama TUI" `
            --add-data "view/css;view/css" `
            --add-data "skills;skills" `
            --hidden-import view.chat `
            --hidden-import view.config `
            --hidden-import view.models `
            --hidden-import view.skills `
            --collect-all llama_index `
            --collect-all rich `
            --collect-all textual `
            --additional-hooks-dir "./config/" `
            --noconfirm `
            main.py

      - name: Fazer upload do artefato
        uses: actions/upload-artifact@v4
        with:
          name: Ollama TUI
          path: dist/Ollama TUI
